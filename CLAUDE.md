# 협업 방식 (중요)

**아이디어를 제안하거나 "이렇게 하면 어떨까?", "어케 생각해?", "~려나?" 같은 질문을 던지면, 그건 상의하자는 뜻이지 바로 구현하라는 뜻이 아니다.**
먼저 말로 의견을 주고받고, 사용자가 "해줘", "진행해줘", "고고", "ㅇㅇ" 처럼 명시적으로 승인한 뒤에만 코드를 수정한다. 작은 수정이라도, 방향이 명백히 좋아 보여도 예외 없음.

같은 이유로 서술형 질문("이게 뭐야?", "~인가?")에도 코드를 건드리지 말고 말로 먼저 답한다.

## 프로젝트 개요

- **어디살까(odilive)**: 자취생 대상 동네 추천 지도 서비스. 1인 사이드 프로젝트(개발자 아님).
- 정적 2-페이지 구조, 빌드 없음: `index.html`(공개, 모바일 대응) / `admin.html`(관리자 전용, **데스크톱만 신경쓰면 됨** — 모바일 대응 불필요).
- 백엔드: Supabase(Postgres + Auth), 지도: Kakao Maps JS SDK.
- GitHub Pages 배포 (repo: odilive/odilive). 커밋/푸시는 사용자가 명시적으로 요청할 때만.
